{
  "version": 1,
  "acceptance_criteria": [
    {"id": "AC-001", "required": true, "expected": "All valid sequences are printed exactly once in lexicographic order."},
    {"id": "AC-002", "required": true, "expected": "N=2, K=200000 completes without quadratic dead-branch enumeration and prints 100001 lines."},
    {"id": "AC-003", "required": true, "expected": "N=1 and the provided small samples retain their exact output."}
  ],
  "risks": [
    {"id": "RISK-LOGIC-001", "type": "correctness"},
    {"id": "RISK-PERF-001", "type": "performance"}
  ],
  "selected_checks": [
    {"id": "CHECK-001", "risk_ids": ["RISK-LOGIC-001"], "required": true, "instrument": "sample comparison"},
    {"id": "CHECK-002", "risk_ids": ["RISK-PERF-001"], "required": true, "instrument": "adversarial timeout and output count"},
    {"id": "CHECK-003", "risk_ids": ["RISK-LOGIC-001"], "required": true, "instrument": "compiler"}
  ],
  "results": [
    {
      "id": "AC-001",
      "status": "PASS",
      "evidence": [
        "Command: g++ -std=c++17 -O2 -Wall -Wextra -pedantic d.cpp -o /tmp/abc473_d",
        "Command: run /tmp/abc473_d against test_d/sample-1.in, sample-2.in, and sample-3.in, then cmp each output",
        "Outcome: exit 0; all three outputs matched the checked-in expected files exactly."
      ]
    },
    {
      "id": "AC-002",
      "status": "PASS",
      "evidence": [
        "Command: printf '2 200000\\n' | timeout 10s /tmp/abc473_d",
        "Outcome: exit 0; produced 100001 lines in 0.10s in the sandbox."
      ]
    },
    {
      "id": "AC-003",
      "status": "PASS",
      "evidence": [
        "Command: exact comparison for test_d/sample-1.in, test_d/sample-2.in, and test_d/sample-3.in",
        "Outcome: exit 0; edge case N=1 and all provided samples matched exactly."
      ]
    },
    {
      "id": "CHECK-001",
      "status": "PASS",
      "evidence": [
        "Command: cmp each generated sample output with test_d/sample-*.out",
        "Outcome: exit 0 for all three sample comparisons."
      ]
    },
    {
      "id": "CHECK-002",
      "status": "PASS",
      "evidence": [
        "Command: timeout 10s on input 2 200000 and wc -l output",
        "Outcome: exit 0; 100001 output lines."
      ]
    },
    {
      "id": "CHECK-003",
      "status": "PASS",
      "evidence": [
        "Command: g++ -std=c++17 -O2 -Wall -Wextra -pedantic d.cpp -o /tmp/abc473_d",
        "Outcome: exit 0; compilation succeeded without warnings."
      ]
    }
  ],
  "residual_risks": []
}
